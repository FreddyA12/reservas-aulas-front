version: '3.8'

services:
  reservas-aulas-front:
    image: reservas-aulas-front:latest
    container_name: reservas-aulas-front
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9060:5173"
